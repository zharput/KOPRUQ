/**
 * Rules Engine - deterministic feasibility checks with explainable
 * reject reasons (spec section 11). {@link com.spanova.rules.EngineeringRule},
 * {@link com.spanova.rules.RuleEvaluation} and {@link com.spanova.rules.RuleEngine}
 * are the P04 infrastructure; {@link com.spanova.rules.RuleEngine} runs with
 * an EMPTY rule set by default.
 *
 * No rule content exists yet, and none should be added without the
 * engineer's explicit approval, source/reference, units, assumptions
 * and a validation-example test (spec section 22). See
 * docs/roadmap.md before adding anything here.
 */
package com.spanova.rules;
