namespace Spanova.Cost;

/// <summary>
/// Placeholder. Not implemented in Prototype P01.
/// Intended future scope (spec section 9): construction cost estimation
/// per alternative, feeding the multi-objective optimization engine.
/// Any unit cost figures introduced later must carry a source/date per
/// spec section 15 - none are invented here.
/// </summary>
public static class ModuleStatus
{
    public const string Status = "Not implemented (Prototype P01 scope excludes cost estimation).";
}
