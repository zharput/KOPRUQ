# AI Service (placeholder)

**Not implemented.** Per the engineer's architecture table and spec
section 17 ("AI is NOT the first development priority. AI shall be
added only after the deterministic computational workflow works"), this
Python/FastAPI service is deferred until after P01 - implemented at P11
(AI Agent).

Planned responsibility: natural-language request -> validated,
deterministic SPANOVA parameters (never a structural-safety authority -
spec section 17, 22: AI does not determine structural safety, does not
invent engineering equations, does not silently modify assumptions).
Also explanation/RAG over results and reports.
