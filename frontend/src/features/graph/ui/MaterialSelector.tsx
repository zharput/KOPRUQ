import { EN_CONCRETE_CLASS_IDS, STRUCTURAL_STEEL_OPTIONS } from '../../materials/model/materialCatalog'
export default function MaterialSelector({ value, steel, disabled, onChange }: { value: string; steel: boolean; disabled?: boolean; onChange: (value: string) => void }) { const options = steel ? STRUCTURAL_STEEL_OPTIONS : EN_CONCRETE_CLASS_IDS.map(value => ({ value, label: value })); return <select className="spn-input" aria-label="Material" value={value} disabled={disabled} onChange={event => onChange(event.target.value)}><option value="" disabled>Select</option>{options.map(option => <option key={option.value} value={option.value}>{option.label}</option>)}</select> }
